version: "2.1"
services:
  sudoku:
    image: ghcr.io/linuxserver/nginx
    container_name: sudoku
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Chicago
    volumes:
      - /opt/docker/volumes/sudoku/config:/config
    ports:
      - 127.0.0.1:4444:4444
    restart: unless-stopped

networks:
  default:
    external:
      name: nginx
