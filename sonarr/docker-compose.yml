version: "3.7"
services:
  sonarr:
    image: ghcr.io/linuxserver/sonarr
    container_name: sonarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Chicago
    volumes:
      - /opt/docker/volumes/sonarr/config:/config
      - /mnt/truenas/Media/Shows:/tv #optional
      - /home/lettucebowler/downloads:/torrents #optional
      - /mnt/truenas/Share/Downloads:/downloads
    restart: unless-stopped

networks:
  default:
    external:
      name: nginx
