version: "2.1"
services:
  components:
    image: ghcr.io/linuxserver/nginx
    container_name: components
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Chicago
    volumes:
      - /opt/docker/volumes/components/config:/config
    restart: unless-stopped

networks:
  default:
    external:
      name: nginx
